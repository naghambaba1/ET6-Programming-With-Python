def mystery_2(a):
    """
    Returns the length of the input if it is non-empty; otherwise, returns None.
    Example: a="nagham" then the output will be 6
    """
    if len(a) == 0:
        return None

    return len(a)
